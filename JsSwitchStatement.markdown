basic syntax which is a little different than Ruby:

    switch(stuff/var/etc) {
      case "something":
      result = "awesome";
      break;
      
      default:
       result = "boring";
    }
